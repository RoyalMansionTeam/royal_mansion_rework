namespace RoyalMasion.Code.Infrastructure.Services.SaveLoadService
{
    public class PlayerProgress
    {
        public PlayerSettings PlayerSettings;
        public LevelProgress LevelProgress;
        public BuildingProgress BuildingProgress;
        public GardenProgress GardenProgress;

        public PlayerProgress()
        {
            /*PlayerSettings = new PlayerSettings();
            LevelProgress = new LevelProgress();
            BuildingProgress = new BuildingProgress();
            GardenProgress = new GardenProgress();*/
        }
    }
}